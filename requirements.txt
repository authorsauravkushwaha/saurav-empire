# SAURAV AI CIVILIZATION — Python runtime
# Deliberately minimal: the civilization must boot with almost nothing installed.
# Layer: kernel + services

fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
PyYAML==6.0.2
python-dotenv==1.0.1
websockets==14.1

# ---- Optional upgrades (install only if you have them) ----
# psycopg[binary]==3.2.3      # PostgreSQL instead of SQLite
# redis==5.2.1                # Redis event bus / cache instead of SQLite queue
# numpy==2.2.1                # faster vector math in memory-service
